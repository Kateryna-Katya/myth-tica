// Ініціалізація іконок
document.addEventListener('DOMContentLoaded', () => {
    lucide.createIcons();

    // Тут будемо додавати логіку мобільного меню та анімацій
    console.log('Myth-Tica scripts loaded. Технология нового поколения активирована.');
});